import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
/trender(<App />);
/tconst linkElement = screen.getByText(/learn react/i);
/texpect(linkElement).toBeInTheDocument();
});
